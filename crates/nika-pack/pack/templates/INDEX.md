<!-- SPDX-License-Identifier: Apache-2.0 -->
<!-- GENERATED by scripts/template-rehearsals.py; edit templates and rehearsals.yaml. -->
# Native authoring index

Discover the installed lessons with `nika try --all` and the skeletons in this index.
With a Compile-capable engine, preview an exact skeleton with `nika compile <template>`; answer its questions before materializing a candidate at an explicit `.nika.yaml` destination.
The filled lessons below reuse the same workflow logic. Rehearse with `--model mock/echo`; model quality needs a separate evaluation.

| Template | Purpose | Filled lesson |
|---|---|---|
| `agent-loop` | plan fast → execute with a leashed agent → validate the typed result | — |
| `aggregate-by-key` | group validated integer amounts with exact totals | `25-aggregate-by-key` |
| `api-upload-and-create` | upload a file and create the resource in one authenticated call | — |
| `bounded-batch` | admit a finite batch before starting model calls | `18-bounded-batch` |
| `chain` | gather facts → one model step → persist | — |
| `classify-and-route` | typed facts → evidence → decision → queue | — |
| `corpus-qa` | canonicalize corpus → retrieve → answer or abstain | — |
| `deduplicate-records` | collapse identical duplicates and refuse conflicts | `21-deduplicate-records` |
| `docker-report` | read real state via a pinned CLI → explain it → keep the report | — |
| `document-to-fields` | source text → anchored facts → nonempty proof | — |
| `etl-state` | incremental data job with a state file | — |
| `evaluate-and-optimize` | draft → two fixed critique/revision rounds | — |
| `fanout` | discover a collection → process every item in parallel → merge | — |
| `gate-and-act` | check a condition, act only when it holds | — |
| `human-gated-ship` | gates in parallel → a human signs → ship · evidence always lands | — |
| `media-asset-pack` | brief → generate assets → manifest | — |
| `parallel-review` | collect independent reviews with a deterministic conjunction | `23-parallel-review` |
| `project-public-fields` | select allowed fields before sending data to a model | `22-project-public-fields` |
| `recover-optional-file` | recover a missing optional file without hiding other failures | `24-recover-optional-file` |
| `snapshot-diff` | compare two snapshots without model judgment | `20-snapshot-diff` |
| `validate-records` | reject malformed records before producing totals | `19-validate-records` |
| `website-brief` | crawl a site → understand it → persist the brief | — |
